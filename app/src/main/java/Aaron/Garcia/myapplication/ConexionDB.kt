package Aaron.Garcia.myapplication

class ConexionDB {
    // 5 - Funciones

    //Método y función son lo mismo, bloques de códigos que se pueden mandar a llamar, sin embargo la diferencia es que un método esta dentro de la clase y la función fuera de esta
    fun conectar(nombreBd: String) {
        //Párametro es lo que espera una función
    }

    fun sumar(num1: Int, num2: Int):Int{

        var suma = num1+num2

        return suma
    }
}

//5.1 Funciones con retorno

fun multiplicar(num1: Int, num2: Int):Int{
    val resultado = num1*num2
    return resultado


}